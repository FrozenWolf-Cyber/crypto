# dependencies
/node_modules

# testing
/coverage

# misc
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# environment variables
.env.local
.env.development.local
.env.test.local
.env.production.local

# optional: keep lock file
# package-lock.json  <-- keep this
# yarn.lock          <-- keep this if using yarn

# build (if you want Koyeb to build)
# /build            <-- comment out if you already built locally
